apiVersion: v1
kind: ResourceQuota
metadata:
  name: api-resource-quota
  namespace: pethealth-prod
spec:
  hard:
    requests.cpu: "1"
    requests.memory: 1Gi
    limits.cpu: "2"
    limits.memory: 2Gi
    pods: "10"
